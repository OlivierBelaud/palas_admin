diff --git a/templates/server/manta-bootstrap.ts b/templates/server/manta-bootstrap.ts
index e728775..f5c6a14 100644
--- a/templates/server/manta-bootstrap.ts
+++ b/templates/server/manta-bootstrap.ts
@@ -60,14 +60,25 @@ async function bootstrap() {
   return _bootstrapped
 }
 
-const _promise = bootstrap()
+let _promise: Promise<any> | null = null
+
+function bootPromise() {
+  if (!_promise) {
+    const current = bootstrap()
+    _promise = current
+    current.catch(() => {
+      if (_promise === current) _promise = null
+    })
+  }
+  return _promise
+}
 
 export async function getMantaAdapter() {
-  const { adapter } = await _promise
+  const { adapter } = await bootPromise()
   return adapter
 }
 
 export async function getMantaApp() {
-  const { app } = await _promise
+  const { app } = await bootPromise()
   return app
 }
