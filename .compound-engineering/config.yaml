schema_version: 1
repository: admin
role: product
platform_manifest: ../platform/workspace.manifest.json
project_tracker:
  provider: linear
  team: OlivierBelaud
  project: PALAS_WORKSPACE
  label: repo:admin
execution:
  isolated_worktree_required: true
  one_executable_ticket_per_repo: true
  cross_repo_parent_label: cross-repo
verification:
  commands:
    - pnpm lint
    - pnpm typecheck
    - pnpm test
    - pnpm build
deployment:
  git_driven: true
  direct_cli_deploy_allowed: false
